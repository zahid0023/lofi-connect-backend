✅ Encapsulation — private constructor + static factory.
✅ Jackson-ready — @JsonInclude, @JsonIgnoreProperties, and precise @JsonAlias mappings.
✅ Smart conversion — using objectMapper.convertValue() instead of manual setters.
✅ Timezone-safe — dynamically applies request.getTimeZone() during mapping.